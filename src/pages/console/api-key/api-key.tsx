import * as React from 'react';

interface IApiKeyProps {
}

const ApiKey: React.FunctionComponent<IApiKeyProps> = (props: IApiKeyProps) => {
  return (
    <>
    api-key    
    </>
  );
};

export default ApiKey;
