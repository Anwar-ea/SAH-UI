import * as React from 'react';

interface IAppListProps {
}

const AppList: React.FunctionComponent<IAppListProps> = (props) => {
  return (
    <>
    app-list
    </>
  );
};

export default AppList;
